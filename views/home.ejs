<%- include('partials/header', { title: "Cây Cảnh Shop" }) %>

<section>
  <h2 class="section-title">Sản phẩm nổi bật</h2>

  <div class="products-grid">
    <% products.forEach(p => { %>
      <div class="product-card">
        <div class="product-img-wrap">
          <img src="<%= p.img %>" alt="<%= p.name %>" class="product-img" />
        </div>
        <div class="product-info">
          <h3 class="product-name"><%= p.name %></h3>
          <div style="font-size:.85rem;color:#f5a623;margin-bottom:.25rem;">
            ⭐ <%= Number(p.avgRating || 0).toFixed(1) %>/5
          </div>
          <div class="product-price"><%= p.price.toLocaleString('vi-VN') %>₫</div>
          <% if (p.description) { %>
            <div class="product-desc"><%= p.description %></div>
          <% } %>
          <div class="card-actions">
            <a href="/product/<%= p._id %>" class="btn-primary">Xem chi tiết</a>
          </div>
        </div>
      </div>
    <% }) %>
  </div>
</section>

<%- include('partials/footer') %>
